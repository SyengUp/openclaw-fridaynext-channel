{
  "id": "friday-next",
  "icon": "https://raw.githubusercontent.com/SyengUp/openclaw-fridaynext-channel/main/assets/icon.png",
  "name": "FridayNext — an iOS client for OpenClaw",
  "description": "FridayNext is an independent iOS app for OpenClaw. It streams every answer live, shows you the thinking behind it, and keeps your agent one tap away — anywhere you go.",
  "channels": ["friday-next"],
  "skills": ["skills"],
  "contracts": {
    "gatewayMethodDispatch": ["authenticated-request"],
    "tools": [
      "fridaynext_health_query",
      "fridaynext_health_log",
      "fridaynext_calendar_query",
      "fridaynext_calendar_log"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "channelConfigs": {
    "friday-next": {
      "label": "Friday Next",
      "description": "HTTP+SSE channel for the Friday iOS client.",
      "schema": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean"
          },
          "logLevel": {
            "type": "string",
            "enum": ["debug", "info", "warn", "error"]
          }
        }
      },
      "uiHints": {
        "logLevel": {
          "label": "Log level",
          "help": "Verbosity of the friday-next plugin's own gateway logs. Raise to `debug` when reporting an issue."
        }
      }
    }
  }
}
